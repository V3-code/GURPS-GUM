<form class="{{cssClass}} gum-item-sheet template-item-sheet" autocomplete="off">
  <header class="sheet-header">
    <img class="profile-img" src="{{item.img}}" data-edit="img" title="{{item.name}}" />
    <div class="header-info">
      <div class="header-main-line">
        <textarea name="name" rows="1" placeholder="{{localize "GUM.TemplateItemSheet.NamePlaceholder"}}">{{item.name}}</textarea>
      </div>
      <div class="header-subline">
        <span class="item-type-tag">{{localize "GUM.TemplateItemSheet.Type"}}</span>
      </div>
      <span class="header-divider" aria-hidden="true"></span>
    </div>
  </header>

  <section class="sheet-content">
    <nav class="sheet-tabs tabs" data-group="primary">
      <a class="item" data-tab="description">{{localize "GUM.TemplateItemSheet.Tabs.Description"}}</a>
      <a class="item" data-tab="structure">{{localize "GUM.TemplateItemSheet.Tabs.Structure"}}</a>
    </nav>

    <section class="sheet-body-content">
      <div class="tab" data-group="primary" data-tab="description">
        <div class="description-tab">
          <div class="description-grid">
            <div class="form-section description-section itemdesc-block">
              <details class="itemdesc-panel" open>
                <summary class="itemdesc-block-header">
                  <h4 class="itemdesc-block-title">{{localize "GUM.TemplateItemSheet.Description.Short"}}</h4>
                </summary>
                {{#if editable}}
                <span class="itemdesc-header-actions">
                    <button type="button" class="toggle-editor itemdesc-edit-button" data-field="system.chat_description">
                      <i class="fas fa-pen"></i>
                    </button>
                </span>
                {{/if}}
                <div class="itemdesc-content">
                  <div class="description-view itemdesc-inner">{{{enrichedChatDescription}}}</div>

                  <div class="description-editor itemdesc-inner" style="display:none;">
                    {{editor system.chat_description target="system.chat_description" engine="prosemirror" owner=owner editable=editable}}
                    <div class="description-actions">
                      <button type="button" class="save-description" data-field="system.chat_description"><i class="fas fa-save"></i> {{localize "GUM.TemplateItemSheet.Common.Save"}}</button>
                      <button type="button" class="expand-description" data-expanded="false"><i class="fas fa-expand"></i> {{localize "GUM.TemplateItemSheet.Common.Expand"}}</button>
                      <button type="button" class="cancel-description"><i class="fas fa-times"></i> {{localize "GUM.TemplateItemSheet.Common.Cancel"}}</button>
                    </div>
                  </div>
                </div>
              </details>
            </div>

            <div class="form-section description-section itemdesc-block">
              <details class="itemdesc-panel" open>
                <summary class="itemdesc-block-header">
                  <h4 class="itemdesc-block-title">{{localize "GUM.TemplateItemSheet.Description.Full"}}</h4>
                </summary>
                {{#if editable}}
                <span class="itemdesc-header-actions">
                    <button type="button" class="toggle-editor itemdesc-edit-button" data-field="system.description">
                      <i class="fas fa-pen"></i>
                    </button>
                </span>
                {{/if}}
                <div class="itemdesc-content">
                  <div class="description-view itemdesc-inner">{{{enrichedDescription}}}</div>

                  <div class="description-editor itemdesc-inner" style="display:none;">
                    {{editor system.description target="system.description" engine="prosemirror" owner=owner editable=editable}}
                    <div class="description-actions">
                      <button type="button" class="save-description" data-field="system.description"><i class="fas fa-save"></i> {{localize "GUM.TemplateItemSheet.Common.Save"}}</button>
                      <button type="button" class="expand-description" data-expanded="false"><i class="fas fa-expand"></i> {{localize "GUM.TemplateItemSheet.Common.Expand"}}</button>
                      <button type="button" class="cancel-description"><i class="fas fa-times"></i> {{localize "GUM.TemplateItemSheet.Common.Cancel"}}</button>
                    </div>
                  </div>
                </div>
              </details>
            </div>
          </div>
        </div>
      </div>

      <div class="tab template-structure-tab" data-group="primary" data-tab="structure">
        <div class="template-blocks-header">
          <h3 class="template-blocks-title">{{localize "GUM.TemplateItemSheet.Structure.Blocks"}}</h3>
          <button type="button" class="add-template-block template-blocks-add" title="{{localize "GUM.TemplateItemSheet.Structure.AddBlock"}}">
            <i class="fas fa-plus"></i>
          </button>
        </div>

        <div class="template-block-list">
          {{#each blocks as |block|}}
          <section class="template-block" data-block-id="{{block.id}}">
            <header class="template-block-header">
              <img class="template-block-icon" src="{{block.icon}}" alt="{{block.typeLabel}}" />

              <div class="template-block-heading">
                <h3 class="template-block-title">{{block.displayTitle}}</h3>
                <div class="template-block-summary">{{block.summaryText}}</div>
              </div>

              <div class="item-controls template-block-controls">
                <a class="item-control edit-template-block" data-block-id="{{block.id}}" title="{{localize "GUM.TemplateItemSheet.Structure.EditBlock"}}">
                  <i class="fas fa-edit"></i>
                </a>
                <a class="item-control delete-template-block" data-block-id="{{block.id}}" title="{{localize "GUM.TemplateItemSheet.Structure.RemoveBlock"}}">
                  <i class="fas fa-trash"></i>
                </a>
              </div>
            </header>

            {{#unless block.collapsed}}
            <div class="template-block-editor">
              <div class="template-block-fields">
                <div class="form-group">
                  <label>{{localize "GUM.TemplateItemSheet.Structure.CustomTitle"}}</label>
                  <input type="text"
                         class="block-field"
                         data-block-id="{{block.id}}"
                         data-field="title"
                         value="{{block.title}}"
                         placeholder="{{localize "GUM.TemplateItemSheet.Structure.CustomTitlePlaceholder"}}"/>
                </div>

                {{#if block.isSelection}}
                <div class="form-group input-narrow">
                  <label>{{localize "GUM.TemplateItemSheet.Structure.ChoiceCount"}}</label>
                  <input type="number"
                         class="block-field"
                         data-block-id="{{block.id}}"
                         data-field="choiceCount"
                         value="{{block.choiceCount}}"
                         min="1"/>
                </div>
                {{/if}}

                {{#if block.isPoints}}
                <div class="form-group input-narrow">
                  <label>{{localize "GUM.TemplateItemSheet.Structure.PointsAvailable"}}</label>
                  <input type="number"
                         class="block-field"
                         data-block-id="{{block.id}}"
                         data-field="pointsAvailable"
                         value="{{block.pointsAvailable}}"
                         min="0"/>
                </div>
                {{/if}}
              </div>



              <div class="template-block-table-wrap">
                <table class="template-block-table">
                  <thead>
                    <tr>
                      <th>{{localize "GUM.TemplateItemSheet.Structure.Name"}}</th>
                      <th>{{localize "GUM.TemplateItemSheet.Structure.QuantityAbbreviation"}}</th>
                      <th>{{localize "GUM.TemplateItemSheet.Structure.Level"}}</th>
                      <th>{{localize "GUM.TemplateItemSheet.Structure.Cost"}}</th>
                      <th>{{localize "GUM.TemplateItemSheet.Structure.Controls"}}</th>
                    </tr>
                  </thead>
                  <tbody>
                    {{#each block.contents as |entry|}}
                    <tr>
                      <td>
                        <div class="template-entry-name">{{entry.rowName}}</div>
                        {{#if entry.rowSubtitle}}
                        <div class="template-entry-subtitle">{{entry.rowSubtitle}}</div>
                        {{/if}}
                      </td>
                      <td>{{entry.rowQty}}</td>
                      <td>{{entry.rowLevel}}</td>
                      <td>{{entry.rowCost}}</td>
                      <td>
                        <div class="template-entry-controls">
                          <a class="edit-block-entry"
                             data-block-id="{{block.id}}"
                             data-entry-id="{{entry.id}}"
                             title="{{localize "GUM.TemplateItemSheet.Common.Edit"}}">
                            <i class="fas fa-edit"></i>
                          </a>
                          <a class="delete-block-entry"
                             data-block-id="{{block.id}}"
                             data-entry-id="{{entry.id}}"
                             title="{{localize "GUM.TemplateItemSheet.Common.Remove"}}">
                            <i class="fas fa-trash"></i>
                          </a>
                        </div>
                      </td>
                    </tr>
                    {{/each}}

                    <tr class="template-drop-row">
                      <td colspan="5">
                        <div class="template-dropzone" data-block-id="{{block.id}}">
                          <i class="fas fa-box-open"></i>
                          <span>
                            {{#if block.contents.length}}
                              {{localize "GUM.TemplateItemSheet.Structure.DropMore"}}
                            {{else}}
                              {{localize "GUM.TemplateItemSheet.Structure.DropEmpty"}}
                            {{/if}}
                          </span>
                        </div>
                      </td>
                    </tr>
                  </tbody>
                </table>
              </div>

                <div class="template-block-actions">
                    <button type="button" class="add-block-attribute" data-block-id="{{block.id}}">
                    <i class="fas fa-sliders-h"></i> {{localize "GUM.TemplateItemSheet.Structure.AddAttribute"}}
                    </button>
                    <button type="button" class="add-block-group" data-block-id="{{block.id}}">
                    <i class="fas fa-project-diagram"></i> {{localize "GUM.TemplateItemSheet.Structure.AddSubgroup"}}
                    </button>
                </div>

            </div>
            {{/unless}}
          </section>
          {{else}}
          <div class="template-empty-state">
            {{localize "GUM.TemplateItemSheet.Structure.Empty"}}
          </div>
          {{/each}}
        </div>
      </div>
    </section>
  </section>
</form>
