<article class="gum-test-request">
  <header>
    <span class="request-card-icon"><i class="fas fa-dice"></i></span>
    <div><span class="request-card-kicker">{{localize "GUM.TestRequest.Card.GMRequest"}}</span><h3>{{request.title}}</h3>
    {{#if request.description}}<p>{{request.description}}</p>{{/if}}
    </div>
  </header>
  <div class="request-summary">
    <span class="request-summary-pill"><i class="fas fa-bullseye"></i> {{testLabel}}</span>
    {{#each purposes}}<span class="request-summary-pill purpose"><i class="fas fa-tag"></i> {{this}}</span>{{/each}}
    {{#if request.test.fixedModifier}}<span class="request-summary-pill modifier"><i class="fas fa-lock"></i> {{request.test.fixedModifier}} {{request.test.fixedModifierLabel}}</span>{{/if}}
  </div>
  <div class="request-progress"><div><span>{{localize "GUM.TestRequest.Card.Progress"}}</span><strong>{{progress.answered}} / {{progress.total}}</strong></div><div class="request-progress-track"><span style="width: {{progressPercent}}%"></span></div></div>
  <div class="request-targets">
    {{#each targets}}
    <details class="request-target-row {{#if unavailableReason}}is-unavailable{{/if}}">
      <summary>
        <img src="{{actorImg}}">
        <span>{{actorName}}</span>
        <strong class="request-result request-result-{{response.outcome}}">{{status}}</strong>
      </summary>
      {{#if response}}
      <dl>
        <dt>{{localize "GUM.TestRequest.Card.Dice"}}</dt><dd>{{response.total}}</dd>
        <dt>{{localize "GUM.TestRequest.Card.EffectiveTarget"}}</dt><dd>{{response.effectiveTarget}}</dd>
        <dt>{{localize "GUM.TestRequest.Card.Margin"}}</dt><dd>{{response.margin}}</dd>
        <dt>{{localize "GUM.TestRequest.Card.Base"}}</dt><dd>{{response.baseValue}}</dd>
        {{#if response.defaultLabel}}<dt>{{localize "GUM.TestRequest.Card.Default"}}</dt><dd>{{response.defaultLabel}}</dd>{{/if}}
        <dt>{{localize "GUM.TestRequest.Card.Modifiers"}}</dt><dd>{{#each modifierBreakdown}}<span class="request-modifier-part">{{label}} {{valueLabel}}</span>{{/each}}</dd>
        {{#if responsePurposeLabels}}<dt>{{localize "GUM.TestRequest.Card.Purposes"}}</dt><dd>{{#each responsePurposeLabels}}{{this}}{{#unless @last}}, {{/unless}}{{/each}}</dd>{{/if}}
        <dt>{{localize "GUM.TestRequest.Card.User"}}</dt><dd>{{responseUserName}}</dd>
        <dt>{{localize "GUM.TestRequest.Card.Time"}}</dt><dd>{{submittedAtLabel}}</dd>
      </dl>
      {{#if hasHistory}}<details class="request-history"><summary>{{historyLabel}}</summary>{{#if previous}}<p>{{localize "GUM.TestRequest.Card.Last"}}: {{previous.status}} · {{localize "GUM.TestRequest.Card.DiceLower"}} {{previous.total}} {{localize "GUM.TestRequest.Card.Versus"}} {{previous.effectiveTarget}}</p>{{/if}}</details>{{/if}}
      {{#unless unavailableReason}}<button type="button" class="test-request-roll" data-target-key="{{encodedKey}}" data-replace="true">{{localize "GUM.TestRequest.Card.Retry"}}</button>{{/unless}}
      {{else}}
        {{#if unavailableReason}}<p class="request-unavailable-reason"><i class="fas fa-triangle-exclamation"></i> {{unavailableReason}}</p>
        {{else}}<button type="button" class="test-request-roll" data-target-key="{{encodedKey}}">{{localize "GUM.TestRequest.Card.RollTest"}}</button>{{/if}}
      {{/if}}
    </details>
    {{/each}}
  </div>
</article>