<form class="template-browser-form gum-browser">
  <div class="browser-container">
    <aside class="browser-sidebar">
      <div class="filter-group">
        <h3 class="filter-title">{{localize "GUM.TemplateBrowser.Search"}}</h3>
        <div class="search-box">
          <input type="text" name="search" placeholder="{{localize "GUM.TemplateBrowser.SearchPlaceholder"}}" />
          <i class="fas fa-search"></i>
        </div>
      </div>

      {{#if folders.length}}
      <div class="filter-group">
        <h3 class="filter-title">{{localize "GUM.TemplateBrowser.FoldersAndSubfolders"}}</h3>
        {{#each folders as |folder|}}
        <label class="filter-check folder-filter-check" style="--folder-depth: {{folder.depth}}">
          <input type="checkbox" name="filter-folder" value="{{folder.id}}" />
          <span>{{folder.name}}</span>
        </label>
        {{/each}}
      </div>
      {{/if}}
    </aside>

    <main class="browser-content">
      <ol class="results-list">
        {{#each templates as |template|}}
        <li class="result-item" data-item-id="{{template.id}}">
          <div class="item-icon">
            {{#if template.displayImg}}
            <img src="{{template.displayImg}}" alt="{{template.name}}" />
            {{/if}}
          </div>

          <div class="item-info">
            <span class="item-name">{{template.name}}</span>
            <small>{{template.sourceLabel}}{{#if template.folderLabel}} • {{template.folderLabel}}{{/if}}</small>
          </div>

          <div class="item-actions">
            <button type="button" class="browser-quick-view" title="{{localize "GUM.TemplateBrowser.ViewTooltip"}}">
              <i class="fas fa-eye"></i>
            </button>
            <label class="item-select" for="template-{{template.id}}">
              <input type="radio" name="selectedTemplate" id="template-{{template.id}}" value="{{template.id}}" />
            </label>
          </div>
        </li>
        {{else}}
        <li class="placeholder-text">{{localize "GUM.TemplateBrowser.NoResults"}}</li>
        {{/each}}
      </ol>
    </main>
  </div>

  <footer class="browser-footer">
    <button type="submit">
      <i class="fas fa-check"></i> {{localize "GUM.TemplateBrowser.ApplyTemplate"}}
    </button>
  </footer>
</form>