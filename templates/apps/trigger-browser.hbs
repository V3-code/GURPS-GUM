{{!-- ✅ Alterado para uma tag <form> para funcionar com FormApplication --}}
<form class="gum-browser">
        <div class="browser-container">
            <aside class="browser-sidebar">
                <div class="filter-group">
                    <h3 class="filter-title">Buscar</h3>
                    <div class="search-box">
                        <input type="text" name="search" placeholder="Buscar gatilho..."/>
                        <i class="fas fa-search"></i>
                    </div>
                </div>

                {{#if folders.length}}
                <div class="filter-group">
                    <h3 class="filter-title">Pastas</h3>
                    {{#each folders as |folder|}}
<label class="filter-check folder-filter-check" style="--folder-depth: {{folder.depth}}"><input type="checkbox" name="filter-folder" value="{{folder.id}}"/> <span>{{folder.name}}</span></label>
                    {{/each}}
                </div>
                {{/if}}
            </aside>

            <main class="browser-content">
                <ol class="results-list">
                    {{#each triggers as |trigger index|}}
                    <li class="result-item" data-item-id="{{trigger.id}}">
                        
                        <div class="item-icon">
                            {{#if trigger.displayImg}}
                                <img src="{{trigger.displayImg}}" alt="{{trigger.name}}">
                            {{/if}}
                        </div>

                        <div class="item-info">
                            <span class="item-name">{{trigger.name}}</span>
                        </div>

                        <div class="item-actions">
                            <button type="button" class="browser-quick-view" data-id="{{trigger.id}}" title="Visualizar gatilho">
                                <i class="fas fa-eye"></i>
                            </button>
                            <label class="item-select" for="trigger-{{trigger.id}}">
                                <input type="radio" name="triggerSelection" value="{{trigger.id}}" id="trigger-{{trigger.id}}" {{#if (eq index 0)}}checked{{/if}}>
                            </label>
                        </div>
                    </li>
                    {{else}}
                    <li class="placeholder-text">Nenhum gatilho encontrado no compêndio "[GUM] Gatilhos".</li>
                    {{/each}}
                </ol>
            </main>
        </div>

        {{!-- ✅ Rodapé com o botão de submit adicionado --}}
        <footer class="browser-footer">
            <button type="submit">
                <i class="fas fa-check"></i> Inserir Gatilho
            </button>
        </footer>
</form>